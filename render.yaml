# Render.com configuration for static site
services:
  - type: web
    name: gallery-website
    env: static
    buildCommand: npm run build
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
